import tape from 'tape';

// import * as testedModule from './handler';

tape('Player creation handler', (functions: tape.Test) => {
  functions.test('travelPlayer()', (cases: tape.Test) => {
    cases.test('travelPlayer()', (test: tape.Test) => {
      test.plan(1);
      test.pass('TBD');
      test.end();
    });
  });
});
